/**
 * Class containing constants for attribute selectors used in custom components.
 */
class AttributeConstants {
	/**
   * CSS selectors for custom attributes used in components.
   * @type {string}
   * @static
   */
	static attributes = "[w-for], [w-on-click], [w-if], [w-on-change], [w-value], [value]";
}

export { AttributeConstants };
